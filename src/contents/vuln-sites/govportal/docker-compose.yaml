services:
  govportal:
    build: .
    image: govportal:vulnerable
    container_name: govportal
    ports:
      - "3002:3002"
    restart: unless-stopped
    networks:
      - 300b-vuln
networks:
  300b-vuln:
    external: true
